# Implementation Plan: View Real Moneydance Data

**Branch**: Feature context `001-view-moneydance-data`; actual git branch at planning is `master` (no branch hook).
**Date**: 2026-09-05 | **Spec**: [spec.md](spec.md)
**Input**: `specs/001-view-moneydance-data/spec.md`

## Summary

Preserve the current UI and add a versioned, financially complete snapshot exported from Moneydance. A worker-owned adapter validates and indexes account-side entries and produces the existing UI's view data. Source-exported compact balance timelines (one baseline at the UTC export date minus one day, plus later changes) supply sidebar values at the user-local page-load date; running balances remain independent of filtering. All Accounts has no total; search includes description/memos/partial amounts and descendants; the sidebar footer displays the local export instant. No cloud delivery, encryption, or deployment is introduced.

## Technical Context

**Language/Version**: Jython 2.7-compatible Moneydance script; browser JavaScript ES modules; Node 20.9.0 available for development tests.
**Primary Dependencies**: Moneydance 2024.4 source model; existing Handlebars with a pinned tested version; native Worker/Intl; development-only schema validator.
**Storage**: Manually provisioned same-origin JSON snapshot, in-memory worker indexes; private data untracked. No financial localStorage/cookies/database.
**Testing**: Node built-in test runner, schema/semantic synthetic fixtures, browser worker/DOM fixture harness, source-register comparisons, Pixel 8 full-history trials.
**Target Platform**: Moneydance on user's Windows desktop; browser on Pixel 8 plus desktop inspection.
**Project Type**: Static read-only web viewer plus in-application export script.
**Performance Goals**: Under two seconds for 20 representative queries; under 10 seconds for five initial-load trials; later decryption remains part of combined target and is not measured here.
**Constraints**: USD safe integer cents; full history; source-accurate per-row balances; no main-thread full snapshot duplication; preserve existing presentation; no raw financial contents in logs.
**Scale/Scope**: One user, approximately 52 MB historical snapshots, active non-investment accounts/categories; hidden account contributions retained.

## Constitution Check

Pre-research originally used the placeholder constitution. Review against constitution v1.1.0: the design preserves financial fidelity, Jython 2.7, privacy, and load-once scope. Source validation remains required. Before implementation dependency adoption, document direct/transitive footprint and reliability/security evidence; no proposed package is approved by this plan alone.
Post-design: PASS. Design retains required full history, mobile running balances, current sidebar semantics, both transfer sides, local export timestamp, and deferred security/cloud scope. Dependency adoption remains gated on constitution VI evidence. Balance timelines remain a proposed design choice, not a mandated abstraction; compare simpler complete-history calculations before implementing them. Runtime financial and performance acceptance gates remain unexecuted, not waived.

## Project Structure

### Documentation (this feature)

```text
specs/001-view-moneydance-data/
  spec.md
  plan.md
  research.md
  data-model.md
  contracts/interfaces.md
  quickstart.md
  checklists/requirements.md
```

`tasks.md` is generated by the next command, not by planning.

### Source Code (repository root)

```text
export_json.py                 # modify source export; no close hook
UI/
  index.html                   # retain templates, expose mobile balance/footer/errors
  styles.css                   # targeted layout only
  app.js                       # DOM controller; no full financial dataset
  config.js                    # planned same-origin snapshot URL
  src/
    snapshot.mjs               # validation + normalization
    money.mjs                  # exact cents validation/format helpers
    query.mjs                  # indexes, filtering, stable paging
    snapshot-worker.mjs        # fetch/parse + worker-owned state
    worker-client.mjs          # one initial load, request IDs, visible errors
    format.mjs                 # export-instant and date-only presentation
  data/                        # private supplied data, existing samples preserved
schemas/snapshot-v1.schema.json # planned maintained export schema
scripts/moneydance-preflight.py # planned source runtime/reference probe
scripts/validate-snapshot.mjs   # planned CLI validation, no data dumps
scripts/serve-ui.mjs            # planned development-only static server
package.json / package-lock.json # planned locked development dependencies
tests/
  fixtures/
  unit/*.test.mjs
  contract/*.test.mjs
  browser/index.html
```

**Structure Decision**: Extend the existing project without a framework migration. Exporter handles accounting semantics; worker handles validated snapshots and queries; main thread handles rendering. The schema and synthetic tests are new implementation deliverables; planning creates none of this source code.

## Design and Delivery Sequence

1. Establish a read-only source preflight on Moneydance 2024.4: capture runtime version and register reference values including same-day ordering, signs, future entries, and hidden contributions. Resolve SDK behavior in the exporter; failed reconciliation blocks financial acceptance.
2. Implement the v1 source contract and atomic export, with semantic validation and synthetic fixtures. Existing legacy snapshots require re-export because required facts are missing; do not claim their current schema is sufficient.
3. Implement pure worker-side validation, exact cents, original-ancestry indexes, immutable account-entry identities, search, and paging. Query state never alters global entries or running balances.
4. Integrate the worker with existing templates: direct register versus descendant search, account identity, mobile running balances, no All Accounts total, error/empty states, and localized export footer.
5. Validate reference finances, browser behaviors and full-volume Pixel 8 timing using quickstart.md. No success claim for future encryption or remote operations.

## Risks and Acceptance Gates

- SDK docs establish candidate methods, not installed-version behavior. Opening balances/order/signs/recursive totals must reconcile; never fabricate zeros or silently drop records.
- A worker may still exceed time or memory limits. Measure initial load and query memory; retain only compact indexes and current page on main thread. Scope changes require explicit discussion.
- Source snapshots must be internally consistent. Abort an export if a stable read cannot be established or source comparisons change during capture.
- ExportDate may be invalid without blocking the independent user-local page-load cutoff; display unavailable timestamp. Missing or invalid balance timelines reject the snapshot. Validate dated hidden-account contributions rather than retaining frozen export-day totals.
- Private files are for local validation only. The development server binds loopback by default; phone validation requires deliberate private-network access with synthetic data first.

## Requirement Coverage

FR-001–004: exporter account coverage + balances and visible tree. FR-005–009: source entry identity/order and query paging, running balances, transfer preservation. FR-010–012: normalized worker search and immutable results. FR-013–014: candidate validation and source reconciliation. FR-015: hide every synthetic total. FR-016: Intl footer tied to the page session snapshot. FR-017: one download per page session, no dataset identity or in-page replacement. SC-001–007: quickstart validation matrix.

## Explicit Test Mode (US1 Acceptance 5–6)

Choose snapshotUrl or testSnapshotUrl in UI/config.js once at page load using the exact test=true query value. Supply a compatible synthetic UI/data/test-snapshot.json and keep the existing UI with a small Test data indicator. A missing real export (HTTP 404) produces a clear error and a link that navigates to the same page with test=true. No automatic fallback or additional fetch occurs in the failing page session. This implements FR-019 and remains within local, read-only viewing scope.
