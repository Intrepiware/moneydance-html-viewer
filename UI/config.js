// Same-origin files, selected once when the page loads. No URL comes from user input.
globalThis.MONEYDANCE_CONFIG = Object.freeze({
  snapshotUrl: './data/snapshot.enc',
  testSnapshotUrl: './data/test-snapshot.json'
});
