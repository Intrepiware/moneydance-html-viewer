I'd now like to start on the next part of the project, which is improving the export from money dance. For this part I'd like to transform the export_json Python script that we created into an actual money dance extension. This extension should be fully installed and should not require reinstallation with every money dance session. The installation should persist if I close down money dance and restart it. The extension should automatically, when money dance closes, run the JSON snapshot export, encrypt it using AES encryption, and upload the encrypted file to blob storage at a specified location. There should also be a menu option inside of money dance to perform these steps on demand. Additionally, there should be a configuration screen where the user can input the encryption password and any credentials and information necessary to upload the file to Azure Blob Storage. Finally, we'll need to update the UI to prompt the user for a password so that the data can be decrypted and loaded. A couple of hard lines with this project: number one I think to get this to work, we will need to create a signing certificate. The AI MUST NOT generate or hallucinate this certificate itself. It must either instruct the user on how to generate this certificate using the prescribed methods, or generate itself using prescribe command-line tools. The extension MUST remain installed when money dance is shut down and restarted. The user should not have to reinstall the extension each time he starts a new money dance session. I prefer that the upload to blob storage does not require an expiring secret, but if it does, the expiration duration MUST be 22 to 24 months. I also prefer the extension give the user a warning close to the expiration date of the expiring secret. Finally, I prefer that the test dataset remain unencrypted. If not, the UI should automatically decrypt it with a hard coded password like the word "password". Since the test file only contains synthetic dummy data, this is an acceptable level of risk.
